// pages/news/news.wxs  时间格式化（WXS 模块）
var SOURCE_MAP = {
  'Motorsport.com': 'Motorsport',
  'motorsport.com': 'Motorsport',
  'motorsport': 'Motorsport',
  'The Race': 'The Race',
  'the_race': 'The Race',
  'Crash.net': 'Crash.net',
  'crash': 'Crash.net',
  'F1i.com': 'F1i',
  'f1i': 'F1i',
  '懂球帝F1': '懂球帝',
  'dongqiudi_f1': '懂球帝',
  '虎扑F1赛车': '虎扑',
  'hupu_f1': '虎扑',
  '微博F1官方': '微博F1',
  'weibo_f1': '微博F1',
}

var fmt = {
  time: function(ts) {
    var diff = Date.now() / 1000 - ts
    if (diff < 3600)   return Math.floor(diff / 60) + '分钟前'
    if (diff < 86400)  return Math.floor(diff / 3600) + '小时前'
    if (diff < 604800) return Math.floor(diff / 86400) + '天前'
    var d = getDate(ts * 1000)
    return (d.getMonth() + 1) + '月' + d.getDate() + '日'
  },
  source: function(s) {
    return SOURCE_MAP[s] || s
  }
}
module.exports = fmt
