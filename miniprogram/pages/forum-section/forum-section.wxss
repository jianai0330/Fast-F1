/* pages/forum-section/forum-section.wxss */
.container { min-height: 100vh; background: #0f0f0f; position: relative; }
.post-scroll { height: 100vh; }

/* 骨架 */
.sk-wrap { padding: 24rpx; }
.sk-card { background: #1a1a1a; border-radius: 16rpx; padding: 28rpx; margin-bottom: 16rpx; }
.sk-line { height: 22rpx; background: #252525; border-radius: 6rpx; margin-bottom: 14rpx; }
.sk-short { width: 40%; } .sk-mid { width: 65%; } .sk-long { width: 88%; }

/* 空状态 */
.empty-wrap { display: flex; flex-direction: column; align-items: center; padding-top: 120rpx; }
.empty-icon { font-size: 80rpx; margin-bottom: 24rpx; }
.empty-text { font-size: 28rpx; color: #555; }

/* 帖子卡片 */
.post-card {
  background: #1a1a1a;
  border-radius: 16rpx;
  margin: 16rpx 24rpx 0;
  padding: 28rpx;
  border: 1rpx solid #242424;
  position: relative;
}

.seeded-badge {
  display: inline-block;
  font-size: 20rpx;
  color: #4a9eff;
  background: rgba(74,158,255,0.12);
  padding: 3rpx 12rpx;
  border-radius: 16rpx;
  margin-bottom: 12rpx;
}

.post-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #e8e8e8;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.55;
  margin-bottom: 20rpx;
}

.post-meta {
  display: flex;
  flex-direction: row;
  align-items: center;
  font-size: 22rpx;
  color: #555;
}

.meta-author { color: #777; max-width: 160rpx; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.meta-dot { margin: 0 8rpx; }
.meta-time { color: #555; }
.meta-right { margin-left: auto; display: flex; flex-direction: row; gap: 20rpx; }
.meta-stat { color: #555; }

/* 发帖悬浮按钮 */
.fab {
  position: fixed;
  right: 40rpx;
  bottom: 60rpx;
  background: #e10600;
  border-radius: 56rpx;
  padding: 20rpx 36rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 10rpx;
  box-shadow: 0 4rpx 24rpx rgba(225,6,0,0.4);
}
.fab-icon { font-size: 32rpx; }
.fab-text { font-size: 26rpx; color: #fff; font-weight: bold; }

/* 加载更多 */
.load-tip { text-align: center; padding: 40rpx 0 120rpx; font-size: 24rpx; }
.text-muted { color: #555; }
